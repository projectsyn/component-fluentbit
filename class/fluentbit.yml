parameters:
  kapitan:
    compile:
      - input_paths:
          - fluentbit/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - fluentbit/component/main.jsonnet
        input_type: jsonnet
        output_path: fluentbit/
