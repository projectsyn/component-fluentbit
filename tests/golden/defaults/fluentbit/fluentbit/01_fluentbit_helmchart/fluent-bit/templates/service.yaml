apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: fluentbit
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: fluent-bit
    app.kubernetes.io/version: 5.1.2
    helm.sh/chart: fluent-bit-0.58.2
  name: fluentbit
  namespace: syn-fluentbit
spec:
  ports:
    - name: http
      port: 2020
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: fluentbit
    app.kubernetes.io/name: fluent-bit
  type: ClusterIP
