parameters:
  fluentbit:
    secret:
      AWS_ACCESS_KEY_ID: <SUPER_SECRET>
      AWS_SECRET_ACCESS_KEY: <EVEN_MORE_SUPERER>

    helm_values:
      kind: Deployment
      replicaCount: 2
      envFrom:
        - secretRef:
            name: ${fluentbit:secretName}
